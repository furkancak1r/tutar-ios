name: UI colour rules

on:
  push:
  pull_request:

jobs:
  colour-rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: sh scripts/check-ui-colors.sh
